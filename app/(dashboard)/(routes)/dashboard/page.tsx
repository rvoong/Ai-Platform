import { Button } from '@/components/ui/button'

const DashboardPage = () => {
  return (
    <Button variant="destructive" size="lg"> Dashboard (Protected) </Button>
  )
}

export default DashboardPage;
